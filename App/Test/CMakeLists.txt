set(CPP main.cpp)
CREATE_EXAMPLE(MYTEST "${CPP}"  LMTC)
set(CMAKE_MFC_FLAG 1)
add_dependencies(MYTEST LMTC)
set_target_properties(MYTEST 
		PROPERTIES
		LINK_FLAGS_DEBUG "/SUBSYSTEM:CONSOLE"
		RUNTIME_OUTPUT_DIRECTORY ${EXE_BIN_DIR}
		)

